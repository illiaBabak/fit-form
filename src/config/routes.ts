class Routes {
  start = "/start";
  redirect = "/redirect";
  login = "/login";
  signUp = "/sign-up";
  confirm = "/confirm";
  main = "/main";
  myPlan = "/my-plan";
}

export const routes = new Routes();
